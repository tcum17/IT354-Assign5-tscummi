import React from 'react';

const SubmitButton = () => {
  return (
    <div>
      <button type="submit" class="btn btn-primary">
        New Post
      </button>
    </div>
  );
};

export default SubmitButton;
